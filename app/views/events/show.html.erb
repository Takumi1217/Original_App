<h1><%= @event.title %></h1>

<div class="event-details">
  <p><i class="glyphicon glyphicon-time"></i> <%= @event.start_date.strftime('%Y年%m月%d日') if @event.start_date.present? %>〜</p>
  <p><i class="glyphicon glyphicon-map-marker"></i> <%= @event.place %></p>
  <div class="d-flex">
    <p><i class="glyphicon glyphicon-road"></i> <%= @event.station %></p>
    <p><i class="glyphicon glyphicon-globe"></i> <%= @event.area %></p>
    <p><i class="glyphicon glyphicon-tags"></i> <%= @event.category %></p>
  </div>
</div>

<h3 class="section-title">基本情報</h3>

<div class="row">
  <div class="col-md-4 text-center">
    <% if @event.thumbnail.attached? %>
      <div class="thumbnail-container">
        <%= image_tag @event.thumbnail, alt: 'Thumbnail', class: 'card-img-top' %>
      </div>
    <% else %>
      <div class="thumbnail-placeholder">No Thumbnail Available</div>
    <% end %>
  </div>

  <div class="col-md-4 text-center">
    <% if @event.image_1.attached? %>
      <div class="thumbnail-container">
        <%= image_tag @event.image_1, alt: 'Image 1', class: 'card-img-top' %>
      </div>
    <% else %>
      <div class="thumbnail-placeholder">No Image Available</div>
    <% end %>
  </div>

  <div class="col-md-4 text-center">
    <% if @event.image_2.attached? %>
      <div class="thumbnail-container">
        <%= image_tag @event.image_2, alt: 'Image 2', class: 'card-img-top' %>
      </div>
    <% else %>
      <div class="thumbnail-placeholder">No Image Available</div>
    <% end %>
  </div>
</div>

<p><strong><%= @event.catchphrase %></strong></p>

<div class="event-body">
  <p><%= @event.body %></p>
</div>

<h3 class="section-title">詳細 <span class="info-subtitle">Info</span></h3>
<hr class="section-divider">
<div class="event-details">
  <div class="detail-row">
    <span class="detail-label">開催場所</span>
    <span class="detail-content">
      <%= @event.place %><br>
    </span>
  </div>

  <div class="detail-row">
    <span class="detail-label">最寄駅</span>
    <span class="detail-content">
      <%= @event.station %>
    </span>
  </div>

  <div class="detail-row">
    <span class="detail-label">開催期間</span>
    <span class="detail-content">
      <%= @event.start_date %> ～ <%= @event.end_date %>
    </span>
  </div>

  <div class="detail-row">
    <span class="detail-label">連絡先</span>
    <span class="detail-content"><%= @event.contact %></span>
  </div>

  <div class="detail-row">
    <span class="detail-label">費用</span>
    <span class="detail-content"><%= @event.cost %>円</span>
  </div>

  <div class="detail-row">
    <span class="detail-label">公式サイト</span>
    <span class="detail-content">
      <%= link_to 'Link', @event.link, target: '_blank', class: 'link-button' if @event.link.present? %>
    </span>
  </div>
  <p class="note">※掲載内容が変更となっている場合があります。最新情報については、会場・主催者の公式サイト等でご確認ください。</p>
</div>
